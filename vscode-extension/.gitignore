# Build artifacts — populated by `npm run build` / vscode:prepublish
# These are copies of repo-root files; the repo root is the source of truth.
resources/
LICENSE
icon.png

# Node modules (if any are added later)
node_modules/
.vscode-test/

# Packaged extension
*.vsix
